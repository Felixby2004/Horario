generator client {
  provider      = "prisma-client-js"
  output        = "../generated/prisma-client-next"
  binaryTargets = ["native", "linux-musl", "linux-musl-openssl-3.0.x", "debian-openssl-1.1.x", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Facultad {
  id_facultad    Int                     @id @default(autoincrement())
  codigo         String                  @unique @db.VarChar(20)
  nombre         String                  @db.VarChar(150)
  activo         Boolean                 @default(true)
  fecha_creacion DateTime                @default(now())
  departamentos  DepartamentoAcademico[]
  docentes       Docente[]

  @@map("facultad")
}

model DepartamentoAcademico {
  id_departamento Int           @id @default(autoincrement())
  id_facultad     Int
  codigo          String        @unique @db.VarChar(20)
  nombre          String        @db.VarChar(150)
  activo          Boolean       @default(true)
  fecha_creacion  DateTime      @default(now())
  facultad        Facultad      @relation(fields: [id_facultad], references: [id_facultad])
  cursos          Curso[]
  docentes        Docente[]
  planes_estudio  PlanEstudio[]

  @@map("departamento_academico")
}

model Usuario {
  id_usuario            Int                           @id @default(autoincrement())
  codigo                String                        @unique @db.VarChar(20)
  nombres               String                        @db.VarChar(100)
  apellidos             String                        @db.VarChar(100)
  correo_electronico    String?                       @unique @db.VarChar(100)
  contrasena_hash       String                        @db.VarChar(255)
  rol                   TipoRol
  activo                Boolean                       @default(true)
  ultimo_acceso         DateTime?
  fecha_creacion        DateTime                      @default(now())
  fecha_actualizacion   DateTime                      @default(now()) @updatedAt
  auditorias            AuditoriaHorario[]
  conflictos_resueltos  ConflictoHorario[]            @relation("ResueltosPor")
  docente               Docente?
  ediciones_docente     HistorialEdicionDocente[]     @relation("UsuarioEditorDocente")
  importaciones_docente HistorialImportacionDocente[] @relation("UsuarioImportacionDocente")
  horarios_creados      HorarioAsignado[]             @relation("CreadosPor")
  cargas_aprobadas      CargaAcademica[]              @relation("CargasAprobadas")
  historial_carga       HistorialCargaAcademica[]
  planes_modificados    PlanEstudio[]                 @relation("UsuarioModificadorPlan")
  versiones_plan        HistorialVersionPlanEstudio[] @relation("UsuarioVersionPlan")

  @@index([codigo])
  @@index([correo_electronico])
  @@map("usuario")
}

model PlanEstudio {
  id_plan                   Int                           @id @default(autoincrement())
  codigo                    String                        @unique @db.VarChar(50)
  nombre                    String                        @db.VarChar(150)
  anio_creacion             Int
  anio_vigencia             Int
  estado                    Boolean                       @default(true)
  resolucion_aprobacion     String?                       @db.VarChar(150)
  id_departamento           Int?
  version_actual            Int                           @default(1)
  descripcion_cambios       String?                       @db.VarChar(255)
  fecha_creacion            DateTime                      @default(now())
  fecha_actualizacion       DateTime                      @default(now()) @updatedAt
  fecha_ultima_modificacion DateTime?
  id_usuario_modificador    Int?
  departamento              DepartamentoAcademico?        @relation(fields: [id_departamento], references: [id_departamento])
  usuario_modificador       Usuario?                      @relation("UsuarioModificadorPlan", fields: [id_usuario_modificador], references: [id_usuario], onDelete: SetNull)
  versiones                 HistorialVersionPlanEstudio[]

  @@index([codigo])
  @@index([id_departamento])
  @@map("plan_estudio")
}

model HistorialVersionPlanEstudio {
  id_version               Int         @id @default(autoincrement())
  id_plan                  Int
  numero_version           Int
  fecha_modificacion       DateTime    @default(now())
  id_usuario_responsable   Int?
  descripcion_cambios      String?     @db.VarChar(255)
  snapshot_antes           Json?
  snapshot_despues         Json
  cursos_agregados         Json?
  cursos_eliminados        Json?
  cursos_modificados       Json?
  restaurada_desde_version Int?
  plan                     PlanEstudio @relation(fields: [id_plan], references: [id_plan], onDelete: Cascade)
  usuario_responsable      Usuario?    @relation("UsuarioVersionPlan", fields: [id_usuario_responsable], references: [id_usuario], onDelete: SetNull)

  @@index([id_plan, numero_version])
  @@map("historial_version_plan_estudio")
}

model PeriodoAcademico {
  id_periodo                    Int                        @id @default(autoincrement())
  codigo                        String                     @unique @db.VarChar(10)
  nombre                        String                     @db.VarChar(100)
  anio                          Int
  semestre                      Int
  fecha_inicio                  DateTime                   @db.Date
  fecha_fin                     DateTime                   @db.Date
  fecha_inicio_clases           DateTime?                  @db.Date
  fecha_fin_clases              DateTime?                  @db.Date
  activo                        Boolean                    @default(true)
  estado                        EstadoPeriodo              @default(planificacion)
  fecha_creacion                DateTime                   @default(now())
  citaciones_docentes           CitacionDocente[]
  conflictos                    ConflictoHorario[]
  dias_no_laborables            DiaNoLaborable[]
  disponibilidad_docentes       DisponibilidadDocente[]
  fase_disponibilidad           FaseDisponibilidad?
  grupos                        Grupo[]
  horarios                      HorarioAsignado[]
  preasignaciones               Preasignacion[]
  restricciones_institucionales RestriccionInstitucional[]
  selecciones_temporales        SeleccionTemporalHorario[]
  ventanas_atencion             VentanaAtencion[]
  cargas_academicas             CargaAcademica[]

  @@map("periodo_academico")
}

model Docente {
  id_docente                Int                               @id @default(autoincrement())
  id_usuario                Int?                              @unique
  id_facultad               Int?
  id_departamento           Int?
  codigo_docente            String                            @unique @db.VarChar(20)
  nombres                   String                            @db.VarChar(100)
  apellidos                 String                            @db.VarChar(100)
  modalidad                 TipoModalidad
  categoria                 TipoCategoria
  categoria_ordinaria       TipoCategoriaOrdinaria?
  tipo_contrato             TipoContratoDocente?
  tipo_extraordinario       TipoExtraordinario?
  dedicacion                TipoDedicacion?
  antiguedad                Int                               @default(0)
  fecha_ingreso             DateTime?                         @db.Date
  correo_electronico        String?                           @db.VarChar(100)
  telefono                  String?                           @db.VarChar(20)
  grado_academico           String?                           @db.VarChar(50)
  especialidad              String?                           @db.VarChar(100)
  horas_maximas_semanales   Int                               @default(40)
  activo                    Boolean                           @default(true)
  fecha_creacion            DateTime                          @default(now())
  fecha_actualizacion       DateTime                          @default(now()) @updatedAt
  direccion                 String?                           @db.VarChar(200)
  disponibilidad            String?
  escuela_profesional       String?                           @db.VarChar(150)
  foto_perfil               String?                           @db.VarChar(255)
  perfil_completo           Boolean                           @default(false)
  horas_totales_asignadas   Int                               @default(0)
  dni_docente               String?                           @unique @db.VarChar(12)
  tipo_dedicacion_laboral   TipoDedicacionLaboral?
  facultad                  Facultad?                         @relation(fields: [id_facultad], references: [id_facultad])
  departamento              DepartamentoAcademico?            @relation(fields: [id_departamento], references: [id_departamento])
  citaciones                CitacionDocente[]
  cola_notificaciones       ColaNotificaciones[]
  conflictos_1              ConflictoHorario[]                @relation("Docente1")
  conflictos_2              ConflictoHorario[]                @relation("Docente2")
  disponibilidades          DisponibilidadDocente[]
  disponibilidades_registro DisponibilidadDocenteRegistro[]
  usuario                   Usuario?                          @relation(fields: [id_usuario], references: [id_usuario])
  cursos                    DocenteCurso[]
  grupos                    DocenteGrupo[]
  historial_notificaciones  HistorialNotificaciones[]
  historial_ediciones       HistorialEdicionDocente[]
  horarios                  HorarioAsignado[]
  preasignaciones           Preasignacion[]
  preferencias_notificacion PreferenciasNotificacionDocente[]
  selecciones_temporales    SeleccionTemporalHorario[]
  cargas_academicas         CargaAcademica[]

  @@index([id_usuario])
  @@map("docente")
}

model HistorialEdicionDocente {
  id_historial_edicion Int      @id @default(autoincrement())
  id_docente           Int
  id_usuario_editor    Int?
  datos_anteriores     Json
  datos_nuevos         Json
  resumen_cambios      Json?
  motivo               String?  @db.VarChar(255)
  fecha_edicion        DateTime @default(now())
  docente              Docente  @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  usuario_editor       Usuario? @relation("UsuarioEditorDocente", fields: [id_usuario_editor], references: [id_usuario])

  @@index([id_docente, fecha_edicion(sort: Desc)])
  @@index([id_usuario_editor])
  @@map("historial_edicion_docente")
}

model HistorialImportacionDocente {
  id_importacion         Int      @id @default(autoincrement())
  id_usuario_responsable Int?
  nombre_archivo         String   @db.VarChar(255)
  formato_archivo        String   @db.VarChar(10)
  total_registros        Int      @default(0)
  registros_validos      Int      @default(0)
  registros_importados   Int      @default(0)
  registros_error        Int      @default(0)
  estado                 String   @db.VarChar(30)
  detalle_resultado      Json?
  fecha_creacion         DateTime @default(now())
  usuario_responsable    Usuario? @relation("UsuarioImportacionDocente", fields: [id_usuario_responsable], references: [id_usuario])

  @@index([fecha_creacion(sort: Desc)])
  @@index([id_usuario_responsable])
  @@map("historial_importacion_docente")
}

model Curso {
  id_curso               Int                        @id @default(autoincrement())
  id_departamento        Int?
  codigo                 String                     @unique @db.VarChar(20)
  nombre                 String                     @db.VarChar(150)
  tipo_curso             String?                    @db.VarChar(20)
  escuela_profesional    String?                    @db.VarChar(150)
  horas_teoria           Int                        @default(0)
  horas_laboratorio      Int                        @default(0)
  horas_practica         Int                        @default(0)
  creditos               Int
  ciclo                  Int?
  plan_estudios          String?                    @db.VarChar(50)
  prerequisitos          String?
  activo                 Boolean                    @default(true)
  fecha_creacion         DateTime                   @default(now())
  conflictos             ConflictoHorario[]
  departamento           DepartamentoAcademico?     @relation(fields: [id_departamento], references: [id_departamento])
  ambientes              CursoAmbiente[]
  docentes               DocenteCurso[]
  prerequisitos_relacion CursoPrerequisito[]        @relation("CursoTienePrerequisitos")
  es_prerrequisito_de    CursoPrerequisito[]        @relation("CursoEsPrerequisito")
  grupos                 Grupo[]
  horarios               HorarioAsignado[]
  preasignaciones        Preasignacion[]
  selecciones_temporales SeleccionTemporalHorario[]

  @@map("curso")
}

model CursoPrerequisito {
  id_curso              Int
  id_curso_prerequisito Int
  fecha_creacion        DateTime @default(now())
  curso                 Curso    @relation("CursoTienePrerequisitos", fields: [id_curso], references: [id_curso], onDelete: Cascade)
  prerequisito          Curso    @relation("CursoEsPrerequisito", fields: [id_curso_prerequisito], references: [id_curso], onDelete: Restrict)

  @@id([id_curso, id_curso_prerequisito])
  @@index([id_curso_prerequisito])
  @@map("curso_prerequisito")
}

model DocenteCurso {
  id_docente_curso  Int       @id @default(autoincrement())
  id_docente        Int
  id_curso          Int
  tipo_clase        TipoClase
  experiencia_anios Int?      @default(0)
  prioridad         Int       @default(1)
  activo            Boolean   @default(true)
  fecha_asignacion  DateTime  @default(now())
  horas_asignadas   Int       @default(0)
  curso             Curso     @relation(fields: [id_curso], references: [id_curso], onDelete: Cascade)
  docente           Docente   @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)

  @@unique([id_docente, id_curso, tipo_clase])
  @@map("docente_curso")
}

model DocenteGrupo {
  id_docente_grupo Int      @id @default(autoincrement())
  id_docente       Int
  id_grupo         Int
  activo           Boolean  @default(true)
  fecha_asignacion DateTime @default(now())
  docente          Docente  @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  grupo            Grupo    @relation(fields: [id_grupo], references: [id_grupo], onDelete: Cascade)

  @@unique([id_docente, id_grupo])
  @@map("docente_grupo")
}

model Grupo {
  id_grupo               Int                        @id @default(autoincrement())
  id_curso               Int
  id_periodo             Int
  codigo_grupo           String                     @db.VarChar(10)
  capacidad_maxima       Int                        @default(40)
  cantidad_matriculados  Int?                       @default(0)
  activo                 Boolean                    @default(true)
  observaciones          String?
  fecha_creacion         DateTime                   @default(now())
  docentes               DocenteGrupo[]
  curso                  Curso                      @relation(fields: [id_curso], references: [id_curso])
  periodo                PeriodoAcademico           @relation(fields: [id_periodo], references: [id_periodo])
  horarios               HorarioAsignado[]
  preasignaciones        Preasignacion[]
  selecciones_temporales SeleccionTemporalHorario[]

  @@unique([id_curso, codigo_grupo, id_periodo])
  @@index([id_curso, id_periodo])
  @@map("grupo")
}

model Ambiente {
  id_ambiente            Int                        @id @default(autoincrement())
  codigo                 String                     @unique @db.VarChar(20)
  nombre                 String                     @db.VarChar(100)
  tipo                   TipoAmbiente
  capacidad              Int
  piso                   String?                    @db.VarChar(10)
  pabellon               String?                    @db.VarChar(50)
  equipamiento           String?
  caracteristicas        Json?
  activo                 Boolean                    @default(true)
  requiere_mantenimiento Boolean                    @default(false)
  observaciones          String?
  fecha_creacion         DateTime                   @default(now())
  fecha_actualizacion    DateTime                   @default(now()) @updatedAt
  conflictos             ConflictoHorario[]
  cursos                 CursoAmbiente[]
  horarios               HorarioAsignado[]
  preasignaciones        Preasignacion[]
  selecciones_temporales SeleccionTemporalHorario[]

  @@map("ambiente")
}

model CursoAmbiente {
  id_curso_ambiente Int       @id @default(autoincrement())
  id_curso          Int
  id_ambiente       Int
  tipo_clase        TipoClase
  ambiente          Ambiente  @relation(fields: [id_ambiente], references: [id_ambiente], onDelete: Cascade)
  curso             Curso     @relation(fields: [id_curso], references: [id_curso], onDelete: Cascade)

  @@unique([id_curso, id_ambiente, tipo_clase])
  @@map("curso_ambiente")
}

model VentanaAtencion {
  id_ventana           Int                          @id @default(autoincrement())
  id_periodo           Int
  fecha                DateTime                     @db.Date
  orden_prioridad      Int
  modalidad            TipoModalidad
  categoria            TipoCategoria
  hora_inicio          String                       @db.VarChar(5)
  hora_fin             String                       @db.VarChar(5)
  intervalo_minutos    Int                          @default(15)
  cantidad_docentes    Int?                         @default(0)
  cantidad_atendidos   Int?                         @default(0)
  completado           Boolean                      @default(false)
  activo               Boolean                      @default(true)
  fecha_creacion       DateTime                     @default(now())
  citaciones           CitacionDocente[]
  configuracion_turnos ConfiguracionTurnosAtencion?
  horarios             HorarioAsignado[]
  periodo              PeriodoAcademico             @relation(fields: [id_periodo], references: [id_periodo])

  @@unique([id_periodo, orden_prioridad, modalidad, categoria], map: "ventana_atencion_id_periodo_orden_prioridad_modalidad_categoria")
  @@index([id_periodo])
  @@map("ventana_atencion")
}

model HorarioAsignado {
  id_asignacion       Int                @id @default(autoincrement())
  id_docente          Int
  id_curso            Int
  id_grupo            Int
  tipo_clase          TipoClase
  id_ambiente         Int
  dia_semana          Int
  hora_inicio         String             @db.VarChar(5)
  hora_fin            String             @db.VarChar(5)
  id_periodo          Int
  id_ventana          Int?
  estado              EstadoHorario      @default(borrador)
  observaciones       String?
  creado_por          Int?
  fecha_creacion      DateTime           @default(now())
  fecha_actualizacion DateTime           @default(now()) @updatedAt
  auditorias          AuditoriaHorario[]
  usuario_creador     Usuario?           @relation("CreadosPor", fields: [creado_por], references: [id_usuario])
  ambiente            Ambiente           @relation(fields: [id_ambiente], references: [id_ambiente])
  curso               Curso              @relation(fields: [id_curso], references: [id_curso])
  docente             Docente            @relation(fields: [id_docente], references: [id_docente])
  grupo               Grupo              @relation(fields: [id_grupo], references: [id_grupo])
  periodo             PeriodoAcademico   @relation(fields: [id_periodo], references: [id_periodo])
  ventana             VentanaAtencion?   @relation(fields: [id_ventana], references: [id_ventana])

  @@index([id_periodo])
  @@index([id_ventana])
  @@index([id_ambiente, id_periodo, estado], map: "idx_horario_ambiente_periodo")
  @@index([id_docente, id_periodo, estado], map: "idx_horario_docente_periodo")
  @@index([id_periodo, estado, dia_semana], map: "idx_horario_periodo_dia")
  @@map("horario_asignado")
}

model SeleccionTemporalHorario {
  id_seleccion     Int              @id @default(autoincrement())
  id_docente       Int
  id_curso         Int
  id_grupo         Int
  tipo_clase       TipoClase
  id_ambiente      Int
  dia_semana       Int
  hora_inicio      String           @db.VarChar(5)
  hora_fin         String           @db.VarChar(5)
  id_periodo       Int
  sesion_id        String           @db.VarChar(100)
  fecha_seleccion  DateTime         @default(now())
  fecha_expiracion DateTime
  ambiente         Ambiente         @relation(fields: [id_ambiente], references: [id_ambiente])
  curso            Curso            @relation(fields: [id_curso], references: [id_curso])
  docente          Docente          @relation(fields: [id_docente], references: [id_docente])
  grupo            Grupo            @relation(fields: [id_grupo], references: [id_grupo])
  periodo          PeriodoAcademico @relation(fields: [id_periodo], references: [id_periodo])

  @@unique([sesion_id, dia_semana, hora_inicio])
  @@index([fecha_expiracion])
  @@map("seleccion_temporal_horario")
}

model DisponibilidadDocente {
  id_disponibilidad  Int              @id @default(autoincrement())
  id_docente         Int
  id_periodo         Int
  dia_semana         Int
  hora_inicio        String           @db.VarChar(5)
  hora_fin           String           @db.VarChar(5)
  disponible         Boolean          @default(true)
  es_restriccion     Boolean          @default(false)
  motivo_restriccion String?          @db.VarChar(200)
  fecha_registro     DateTime         @default(now())
  docente            Docente          @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  periodo            PeriodoAcademico @relation(fields: [id_periodo], references: [id_periodo])

  @@unique([id_docente, dia_semana, hora_inicio, id_periodo], map: "disponibilidad_docente_id_docente_dia_semana_hora_inicio_id_per")
  @@map("disponibilidad_docente")
}

model RestriccionInstitucional {
  id_restriccion   Int               @id @default(autoincrement())
  tipo_restriccion String            @db.VarChar(50)
  nombre           String            @db.VarChar(100)
  configuracion    Json
  id_periodo       Int?
  activo           Boolean           @default(true)
  fecha_creacion   DateTime          @default(now())
  periodo          PeriodoAcademico? @relation(fields: [id_periodo], references: [id_periodo])

  @@map("restriccion_institucional")
}

model DiaNoLaborable {
  id_dia_no_laborable Int                @id @default(autoincrement())
  fecha               DateTime           @db.Date
  descripcion         String             @db.VarChar(200)
  tipo                TipoDiaNoLaborable
  afecta_clases       Boolean            @default(true)
  id_periodo          Int?
  periodo             PeriodoAcademico?  @relation(fields: [id_periodo], references: [id_periodo])

  @@unique([fecha, id_periodo])
  @@map("dia_no_laborable")
}

model Preasignacion {
  id_preasignacion Int                 @id @default(autoincrement())
  id_docente       Int
  id_curso         Int
  id_grupo         Int
  tipo_clase       TipoClase
  id_ambiente      Int?
  dia_semana       Int?
  hora_inicio      String?             @db.VarChar(5)
  hora_fin         String?             @db.VarChar(5)
  id_periodo       Int
  estado           EstadoPreasignacion @default(pendiente)
  observaciones    String?
  fecha_creacion   DateTime            @default(now())
  ambiente         Ambiente?           @relation(fields: [id_ambiente], references: [id_ambiente])
  curso            Curso               @relation(fields: [id_curso], references: [id_curso])
  docente          Docente             @relation(fields: [id_docente], references: [id_docente])
  grupo            Grupo               @relation(fields: [id_grupo], references: [id_grupo])
  periodo          PeriodoAcademico    @relation(fields: [id_periodo], references: [id_periodo])

  @@map("preasignacion")
}

model ConflictoHorario {
  id_conflicto     Int              @id @default(autoincrement())
  id_periodo       Int
  tipo_conflicto   TipoConflicto
  descripcion      String
  id_docente_1     Int?
  id_docente_2     Int?
  id_curso         Int?
  id_ambiente      Int?
  dia_semana       Int?
  hora_inicio      String?          @db.VarChar(5)
  hora_fin         String?          @db.VarChar(5)
  resuelto         Boolean          @default(false)
  fecha_deteccion  DateTime         @default(now())
  fecha_resolucion DateTime?
  resuelto_por     Int?
  ambiente         Ambiente?        @relation(fields: [id_ambiente], references: [id_ambiente])
  curso            Curso?           @relation(fields: [id_curso], references: [id_curso])
  docente_1        Docente?         @relation("Docente1", fields: [id_docente_1], references: [id_docente])
  docente_2        Docente?         @relation("Docente2", fields: [id_docente_2], references: [id_docente])
  periodo          PeriodoAcademico @relation(fields: [id_periodo], references: [id_periodo])
  usuario_resolvio Usuario?         @relation("ResueltosPor", fields: [resuelto_por], references: [id_usuario])

  @@map("conflicto_horario")
}

model AuditoriaHorario {
  id_auditoria     Int              @id @default(autoincrement())
  id_asignacion    Int?
  accion           AccionAuditoria
  usuario_id       Int
  datos_anteriores Json?
  datos_nuevos     Json?
  direccion_ip     String?          @db.VarChar(45)
  motivo           String?
  fecha_registro   DateTime         @default(now())
  asignacion       HorarioAsignado? @relation(fields: [id_asignacion], references: [id_asignacion])
  usuario          Usuario          @relation(fields: [usuario_id], references: [id_usuario])

  @@map("auditoria_horario")
}

model ConfiguracionNotificaciones {
  id_configuracion        Int               @id @default(autoincrement())
  tipo_notificacion       String            @db.VarChar(50)
  canal                   CanalNotificacion
  activo                  Boolean           @default(true)
  plantilla_mensaje       String?
  hora_envio              String?           @db.VarChar(5)
  dias_antelacion         Int?              @default(1)
  minutos_antelacion      Int?
  configuracion_adicional Json?
  fecha_creacion          DateTime          @default(now())
  fecha_actualizacion     DateTime          @default(now()) @updatedAt

  @@unique([tipo_notificacion, canal])
  @@map("configuracion_notificaciones")
}

model PreferenciasNotificacionDocente {
  id_preferencia      Int               @id @default(autoincrement())
  id_docente          Int
  canal               CanalNotificacion
  activo              Boolean           @default(true)
  datos_contacto      Json
  verificado          Boolean           @default(false)
  fecha_verificacion  DateTime?
  fecha_creacion      DateTime          @default(now())
  fecha_actualizacion DateTime          @default(now()) @updatedAt
  docente             Docente           @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)

  @@unique([id_docente, canal])
  @@index([id_docente, canal])
  @@map("preferencias_notificacion_docente")
}

model HistorialNotificaciones {
  id_notificacion    Int               @id @default(autoincrement())
  id_docente         Int
  id_ventana         Int?
  tipo_notificacion  String            @db.VarChar(50)
  canal              CanalNotificacion
  mensaje            String
  estado_envio       EstadoEnvio       @default(pendiente)
  fecha_envio        DateTime?
  fecha_entrega      DateTime?
  codigo_respuesta   String?           @db.VarChar(50)
  respuesta_servidor String?
  intentos           Int               @default(1)
  fecha_creacion     DateTime          @default(now())
  docente            Docente           @relation(fields: [id_docente], references: [id_docente])

  @@index([id_docente, fecha_creacion(sort: Desc)])
  @@index([estado_envio, fecha_creacion])
  @@map("historial_notificaciones")
}

model ColaNotificaciones {
  id_cola             Int               @id @default(autoincrement())
  id_docente          Int
  tipo_notificacion   String            @db.VarChar(50)
  canal               CanalNotificacion
  datos_mensaje       Json
  fecha_programada    DateTime
  prioridad           Int               @default(5)
  estado              EstadoCola        @default(pendiente)
  intentos            Int               @default(0)
  maximo_intentos     Int               @default(3)
  fecha_creacion      DateTime          @default(now())
  fecha_procesamiento DateTime?
  docente             Docente           @relation(fields: [id_docente], references: [id_docente])

  @@index([fecha_programada, estado, prioridad])
  @@map("cola_notificaciones")
}

model FaseDisponibilidad {
  id_fase_disponibilidad   Int                             @id @default(autoincrement())
  id_periodo               Int                             @unique
  estado                   EstadoFaseDisponibilidad        @default(no_iniciada)
  fecha_inicio             DateTime
  fecha_fin                DateTime
  bloques_tiempo           String                          @default("30") @db.VarChar(50)
  instrucciones            String?
  activo                   Boolean                         @default(true)
  fecha_creacion           DateTime                        @default(now())
  fecha_actualizacion      DateTime                        @default(now()) @updatedAt
  registros_disponibilidad DisponibilidadDocenteRegistro[]
  periodo                  PeriodoAcademico                @relation(fields: [id_periodo], references: [id_periodo])

  @@map("fase_disponibilidad")
}

model DisponibilidadDocenteRegistro {
  id_registro           Int                @id @default(autoincrement())
  id_docente            Int
  id_fase               Int
  matriz_disponibilidad Json
  bloques_preferidos    Json?
  notas                 String?            @db.VarChar(500)
  completado            Boolean            @default(false)
  fecha_registro        DateTime           @default(now())
  fecha_actualizacion   DateTime           @default(now()) @updatedAt
  docente               Docente            @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  fase                  FaseDisponibilidad @relation(fields: [id_fase], references: [id_fase_disponibilidad], onDelete: Cascade)

  @@unique([id_docente, id_fase])
  @@map("disponibilidad_docente_registro")
}

model CitacionDocente {
  id_citacion          Int              @id @default(autoincrement())
  id_docente           Int
  id_periodo           Int
  id_ventana           Int
  fecha_citacion       DateTime         @db.Date
  hora_inicio          String           @db.VarChar(5)
  hora_fin             String           @db.VarChar(5)
  numero_orden_turno   Int
  estado               EstadoCitacion   @default(programada)
  confirmado_docente   Boolean          @default(false)
  fecha_confirmacion   DateTime?
  razon_rechazo        String?          @db.VarChar(300)
  observaciones        String?
  notificacion_enviada Boolean          @default(false)
  recordatorio_enviado Boolean          @default(false)
  fecha_creacion       DateTime         @default(now())
  fecha_actualizacion  DateTime         @default(now()) @updatedAt
  docente              Docente          @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  periodo              PeriodoAcademico @relation(fields: [id_periodo], references: [id_periodo])
  ventana              VentanaAtencion  @relation(fields: [id_ventana], references: [id_ventana])

  @@unique([id_docente, id_periodo, id_ventana])
  @@index([fecha_citacion, hora_inicio])
  @@index([estado, notificacion_enviada])
  @@map("citacion_docente")
}

model ConfiguracionTurnosAtencion {
  id_configuracion        Int             @id @default(autoincrement())
  id_ventana              Int             @unique
  minutos_por_turno       Int             @default(15)
  cantidad_turnos         Int
  permitir_reprogramacion Boolean         @default(true)
  dias_previos_minimo     Int             @default(1)
  activo                  Boolean         @default(true)
  fecha_creacion          DateTime        @default(now())
  fecha_actualizacion     DateTime        @default(now()) @updatedAt
  ventana                 VentanaAtencion @relation(fields: [id_ventana], references: [id_ventana], onDelete: Cascade)

  @@map("configuracion_turnos_atencion")
}

model HistorialCitacion {
  id_historial    Int             @id @default(autoincrement())
  id_citacion     Int
  accion          String          @db.VarChar(50)
  estado_anterior EstadoCitacion?
  estado_nuevo    EstadoCitacion
  cambios         Json?
  razon           String?
  usuario_id      Int?
  fecha_registro  DateTime        @default(now())

  @@map("historial_citacion")
}

model ConfiguracionSistema {
  id_configuracion           Int      @id @default(autoincrement())
  bloques_horarios           Int      @default(10)
  duracion_bloque            Int      @default(90)
  hora_inicio                String   @default("07:00") @db.VarChar(5)
  hora_fin                   String   @default("22:00") @db.VarChar(5)
  max_horas_docente          Int      @default(40)
  min_horas_entre_clases     Int      @default(0)
  permitir_clases_seguidas   Boolean  @default(true)
  validar_capacidad_ambiente Boolean  @default(true)
  fecha_creacion             DateTime @default(now())
  fecha_actualizacion        DateTime @default(now()) @updatedAt

  @@map("configuracion_sistema")
}

model actividad_no_lectiva_docente {
  id_actividad_no_lectiva Int       @id @default(autoincrement())
  id_docente              Int
  tipo_actividad          String    @db.VarChar(100)
  cantidad_horas          Int
  descripcion             String?
  fecha_creacion          DateTime? @default(now()) @db.Timestamp(6)
  fecha_actualizacion     DateTime? @default(now()) @db.Timestamp(6)
}

model CargaAcademica {
  id_carga                Int                       @id @default(autoincrement())
  id_docente              Int
  id_periodo              Int
  estado                  EstadoCargaAcademica      @default(borrador)
  horas_lectivas          Int                       @default(0)
  horas_no_lectivas       Int                       @default(0)
  horas_preparacion       Int                       @default(0)
  horas_totales           Int                       @default(0)
  horas_meta              Int                       @default(40)
  observaciones           String?
  observaciones_generales String?
  fecha_envio             DateTime?
  fecha_aprobacion        DateTime?
  aprobado_por            Int?
  fecha_creacion          DateTime                  @default(now())
  fecha_actualizacion     DateTime                  @default(now()) @updatedAt
  docente                 Docente                   @relation(fields: [id_docente], references: [id_docente], onDelete: Cascade)
  periodo                 PeriodoAcademico          @relation(fields: [id_periodo], references: [id_periodo])
  usuario_aprobador       Usuario?                  @relation("CargasAprobadas", fields: [aprobado_por], references: [id_usuario])
  actividades_no_lectivas ActividadNoLectiva[]
  historial               HistorialCargaAcademica[]

  @@index([id_docente, id_periodo])
  @@index([estado])
  @@map("carga_academica")
}

model ActividadNoLectiva {
  id_actividad        Int                    @id @default(autoincrement())
  id_carga            Int
  tipo_actividad      TipoActividadNoLectiva
  nombre              String                 @db.VarChar(200)
  descripcion         String?
  horas_semanales     Int
  horas_asignadas     Int                    @default(0)
  dias_semana         Json?
  fecha_inicio        DateTime?
  fecha_fin           DateTime?
  datos_adicionales   Json?
  datos_sustento      Json?
  horarios_actividad  Json?
  observaciones       String?
  fecha_creacion      DateTime               @default(now())
  fecha_actualizacion DateTime               @default(now()) @updatedAt
  carga_academica     CargaAcademica         @relation(fields: [id_carga], references: [id_carga], onDelete: Cascade)

  @@map("actividad_no_lectiva")
}

model HistorialCargaAcademica {
  id_historial    Int                   @id @default(autoincrement())
  id_carga        Int
  id_usuario      Int
  estado_anterior EstadoCargaAcademica?
  estado_nuevo    EstadoCargaAcademica
  observaciones   String?
  fecha_creacion  DateTime              @default(now())
  carga_academica CargaAcademica        @relation(fields: [id_carga], references: [id_carga], onDelete: Cascade)
  usuario         Usuario               @relation(fields: [id_usuario], references: [id_usuario])

  @@map("historial_carga_academica")
}

enum TipoRol {
  administrador_sistema
  director_escuela
  coordinador_academico
  operador_horarios
  docente

  @@map("tipo_rol")
}

enum EstadoPeriodo {
  planificacion
  asignacion_horarios
  en_curso
  finalizado

  @@map("estado_periodo")
}

enum TipoModalidad {
  nombrado
  contratado
  extraordinario

  @@map("tipo_modalidad")
}

enum TipoCategoriaOrdinaria {
  principal
  asociado
  auxiliar

  @@map("tipo_categoria_ordinaria")
}

enum TipoContratoDocente {
  tipo_a1
  tipo_b1
  tipo_a2
  tipo_b2
  tipo_a3
  tipo_b3
  jefe_practica

  @@map("tipo_contrato_docente")
}

enum TipoExtraordinario {
  cesante
  experto
  emerito
  invitado_especial

  @@map("tipo_extraordinario")
}

enum TipoCategoria {
  principal
  asociado
  auxiliar
  jefe_practica

  @@map("tipo_categoria")
}

enum TipoDedicacion {
  dedicacion_exclusiva
  tiempo_completo
  tiempo_parcial_20
  tiempo_parcial_12
  tiempo_parcial_10
  tiempo_parcial_04
  docente_investigador
  tiempo_parcial_16
  tiempo_parcial_08
  tiempo_parcial
  por_horas

  @@map("tipo_dedicacion")
}

enum TipoClase {
  teoria
  laboratorio
  practica

  @@map("tipo_clase")
}

enum TipoAmbiente {
  aula
  laboratorio
  auditorio
  sala_reuniones

  @@map("tipo_ambiente")
}

enum EstadoHorario {
  borrador
  solicitado
  aprobado
  confirmado
  publicado
  modificado
  cancelado

  @@map("estado_horario")
}

enum TipoDiaNoLaborable {
  feriado_nacional
  feriado_local
  mantenimiento
  institucional
  duelo

  @@map("tipo_dia_no_laborable")
}

enum EstadoPreasignacion {
  pendiente
  confirmada
  cancelada

  @@map("estado_preasignacion")
}

enum TipoConflicto {
  cruce_docente
  cruce_grupo
  cruce_ambiente
  exceso_horas_docente
  ambiente_no_disponible
  fuera_franja

  @@map("tipo_conflicto")
}

enum AccionAuditoria {
  crear
  modificar
  eliminar
  confirmar
  publicar
  cancelar
  reasignar

  @@map("accion_auditoria")
}

enum CanalNotificacion {
  correo
  whatsapp
  telegram

  @@map("canal_notificacion")
}

enum EstadoEnvio {
  pendiente
  enviado
  entregado
  leido
  fallido
  rebotado

  @@map("estado_envio")
}

enum EstadoCola {
  pendiente
  procesando
  completado
  fallido

  @@map("estado_cola")
}

enum EstadoCitacion {
  programada
  confirmada_docente
  rechazada
  completada
  cancelada

  @@map("estado_citacion")
}

enum EstadoFaseDisponibilidad {
  no_iniciada
  abierta
  cerrada
  procesada

  @@map("estado_fase_disponibilidad")
}

enum TipoDedicacionLaboral {
  dedicacion_exclusiva
  tiempo_completo
  tiempo_parcial_20
  por_horas

  @@map("tipo_dedicacion_laboral")
}

enum TipoActividadNoLectiva {
  tutoria_consejeria
  investigacion
  responsabilidad_social
  gestion_gobierno
  asesoria_tesis_jurado
  perfeccionamiento
  preparacion_evaluacion

  @@map("tipo_actividad_no_lectiva")
}

enum EstadoCargaAcademica {
  borrador
  enviado
  en_revision
  observado
  validado
  aprobado
  publicado
  cancelado

  @@map("estado_carga_academica")
}
